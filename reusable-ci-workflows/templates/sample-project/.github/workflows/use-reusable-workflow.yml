name: Use Reusable Workflow

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    uses: ../../workflows/reusable-build.yml
    with:
      # Add any required inputs for the reusable build workflow here
      example_input: 'value'

  test:
    uses: ../../workflows/reusable-test.yml
    with:
      # Add any required inputs for the reusable test workflow here
      example_input: 'value'

  deploy:
    uses: ../../workflows/reusable-deploy.yml
    with:
      # Add any required inputs for the reusable deploy workflow here
      example_input: 'value'